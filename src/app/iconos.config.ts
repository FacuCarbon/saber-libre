import { addIcons } from 'ionicons';
import { add, closeCircle, trash, create, book, person } from 'ionicons/icons';

export const registrarIconos = () => {
  addIcons({
    //
    add,
    closeCircle,
    //
    trash,
    create,
    //
    book,
    person,
  });
};
